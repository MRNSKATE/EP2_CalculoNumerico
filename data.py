def final_calculate(x_sup, x_inf, y_sup, y_inf, f_calc, x_j, n):
  evaluate = f_calc
  x=x_j
  x_inf=float(x_inf)
  x_sup=float(x_sup)
  y_sup=eval(y_sup)
  y_inf=eval(y_inf)
  expression = eval(evaluate)
  return expression
